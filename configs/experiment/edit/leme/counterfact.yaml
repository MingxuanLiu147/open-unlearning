# LEME CounterFact 长文本编辑实验配置

defaults:
  - override /model: Qwen2.5-7B-Instruct
  - override /trainer: edit/ROME
  - override /data: edit
  - override /eval: edit

data:
  edit:
    LEME_counterfact_edit:
      handler: LEMEDataset
      args:
        subset: "counterfact"
        max_length: 768
