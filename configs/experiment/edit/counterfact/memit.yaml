# CounterFact + MEMIT 批量编辑实验配置

defaults:
  - override /model: Qwen2.5-7B-Instruct
  - override /trainer: edit/MEMIT
  - override /data: edit
  - override /eval: edit

data:
  edit:
    CounterFact_edit:
      handler: CounterFactDataset
      args:
        split: "train"
        max_length: 512
