# LoRA for GPT-2
# GPT-2 uses c_attn (fused QKV) and c_proj for attention

defaults:
  - inject/base_inject@_here_
  - _self_

handler: LoRATrainer

method_args:
  r: 8
  lora_alpha: 16
  lora_dropout: 0.05
  target_modules:
    - c_attn
    - c_proj
  bias: none
  task_type: CAUSAL_LM
