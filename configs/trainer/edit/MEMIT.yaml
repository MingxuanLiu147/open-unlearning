# MEMIT 批量知识编辑配置
# Mass-Editing Memory in Transformer

defaults:
  - base_editor

handler: MEMITEditor

method_args:
  # 编辑层（MEMIT 使用多层分散编辑）
  layers: [4, 5, 6, 7, 8]
  
  # Value 优化参数
  v_lr: 0.5
  v_num_grad_steps: 20
  
  # 正则化参数
  clamp_norm_factor: 4.0
  
  # 每层编辑权重
  edit_weight: 0.5
