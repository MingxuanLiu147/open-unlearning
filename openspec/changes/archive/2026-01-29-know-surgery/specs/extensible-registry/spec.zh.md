## 新增需求

### 需求：插件元数据注册表
系统应将元数据与注册的模型、方法和数据集关联，包括任务类型、模态和资源需求。

#### 场景：读取插件元数据
- **当** 组件被注册时
- **那么** 其元数据可供流水线和未来的 GUI 使用，用于验证和选择

### 需求：兼容性验证
系统应在执行前验证模型-方法-数据集的兼容性，并应在遇到不兼容组合时快速失败，提供可操作的错误消息。

#### 场景：拒绝不兼容的组合
- **当** 选择的方法与所选模型或数据集不兼容时
- **那么** 流水线在训练前中止，并报告不兼容的原因
