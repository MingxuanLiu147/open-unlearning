## 新增需求

### 需求：基于 PEFT 的知识注入
系统应支持通过 PEFT 方法进行知识注入，包括 LoRA、DoRA、AdaLoRA、LoReFT 和 BREP-REFT。

#### 场景：运行基于 LoRA 的注入
- **当** 用户选择 LoRA 进行注入并提供数据集时
- **那么** 系统执行注入并使用共享的评估框架报告结果

### 需求：注入评估钩子
系统应为注入任务定义评估钩子，包括适用的多模态指标。

#### 场景：评估注入结果
- **当** 注入评估运行时
- **那么** 系统报告为所选数据集和模态定义的注入指标
