# 任务计划：在 Llama-3.2-1B 上运行 Unlearning Trainers

## 目标
在 `open-unlearning` 中，以对齐现有 NPO 运行方式的执行模式，在 `Llama-3.2-1B-Instruct` 模型上运行 `src/trainer/unlearn` 下的方法，并在实验完成后生成一份配置文档，记录运行设置、命令、模型权重、输出路径，以及必要时的最小代码修改。

## 阶段
- [x] 阶段 1：规划与准备
- [x] 阶段 2：检查可运行配置与依赖
- [ ] 阶段 3：执行训练与评测
- [ ] 阶段 4：整理产物并编写配置文档

## 关键问题
1. `src/trainer/unlearn` 下哪些方法可以在不改代码的前提下直接通过现有 Hydra 配置运行？
2. 哪些数据集与实验预设最接近 NPO 路径，可以最大程度复用于目标方法？
3. 运行 `Llama-3.2-1B-Instruct` 需要满足哪些环境与 checkpoint 前提？
4. 为了保证可复现，最终配置文档需要记录哪些关键输出？

## 已做决定
- 以现有的 NPO 风格配置流作为命令结构和产物收集的基线。
- 除非某个 trainer 无法通过现有配置栈运行，否则不改代码。
- 如果必须改代码，保持最小改动，记录精确文件位置，并与实验产物分开说明。
- 优先复用仓库里已有的命令模式和 Hydra 产物，不额外创建新的启动代码。
- 将 `open-unlearning/tofu_Llama-3.2-1B-Instruct_full` 作为首选基础权重来源，本地已保存 checkpoint 作为回退或 tokenizer 复用途径。
- 由于仓库内未找到 `open-unlearning/tofu_Llama-3.2-1B-Instruct_full` 本地目录，本轮执行前先验证远端可访问性；若不可访问，则切换到本地完整权重 `saves/finetune/tofu_Llama-3.2-1B-Instruct_full_v4`，不改训练代码，只做命令级覆盖。
- 以 `run_all_methods_retry1.log` 和 `执行总表.md` 代表的实际落地配置为准，而不是仅沿用旧脚本里的双卡大 batch 默认值。
- 用户已确认本轮允许使用多卡训练，因此训练优先采用仓库现有 `accelerate + DeepSpeed ZeRO-3` 双卡模式；若个别方法在双卡下不稳定，再单独回退到单卡。
- 本轮新增统一时间戳前缀 `20260313_151846`，所有新结果目录和配置文档文件名都必须带上该时间戳，避免覆盖历史产物。
- 本阶段所有过程文档统一写入 `docs_process/2026-03-11_llama3.2-1B_遗忘实验执行/`。

## 已遇到的问题
- 暂无。

## 当前状态
**目前处于阶段 2/3 交界**：已完成方法级约束检查，正在确认基础权重来源、GPU 分配与最终命令模板；确认后按既定顺序启动训练与评测，并同步沉淀过程文档。
