# 验证记录

## 代码层验证

- `PYTHONPATH=src .venv/bin/python -m py_compile scripts/prepare_logic_reasoning_public_data.py`
- 新增 YAML 配置均可被 `yaml.safe_load` 正常解析
- `webui.utils.config_loader.ConfigLoader(...).get_datasets("inject")` 已能返回：
  - 训练集：`CLUTRR_train_inject`、`RuleTaker_train_inject`、`ProofWriter_train_inject`、`FOLIO_train_inject`、`LogicBench_train_inject`
  - 评测集：`CLUTRR_eval_inject`、`RuleTaker_eval_inject`、`ProofWriter_eval_inject`、`FOLIO_eval_inject`、`LogicBench_eval_inject`、`RuleArena_eval_inject`

## 数据样本量

- CLUTRR：train `53,518`，eval `3,977`
- RuleTaker：train `480,152`，eval `151,911`
- ProofWriter：train `585,552`，eval `174,476`
- FOLIO：train `1,001`，eval `203`
- LogicBench：train `12,908`，eval `2,020`
- RuleArena：eval `816`

## 备注

- FOLIO 本地评测文件来自公开 `validation` split，落盘路径统一为 `eval/folio/test.json`
- RuleArena 只有评测集，没有训练集注册

## 2026-03-19 LoRA 烟雾验证

- 使用真实 `Llama-3.2-1B-Instruct` 本地快照，对 `CLUTRR`、`RuleTaker`、`ProofWriter`、`FOLIO`、`LogicBench`、`RuleArena` 分别执行 `max_steps=1` 的 LoRA 训练烟雾测试
- 结果：`6 / 6` 命令退出码为 `0`
- 详细命令、逐数据集结果和输出目录见 [05_LoRA烟雾验证.md](/home/liumingxuan/open-unlearning/docs_process/2026-03-18_逻辑推理数据集下载与注册/05_LoRA烟雾验证.md)
