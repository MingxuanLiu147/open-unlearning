# 计划

## 目标

按现有 inject 数据接入方式，将 CLUTRR、RuleTaker、ProofWriter、FOLIO、LogicBench、RuleArena 下载到本地、规范化输出、注册到 `configs/data/datasets`，并在 `docs_process/2026-03-18_逻辑推理数据集下载与注册/` 留下最小文档。

## 完成状态

- [x] 确认 6 个 benchmark 的可下载来源和字段结构
- [x] 实现统一准备脚本 `scripts/prepare_logic_reasoning_public_data.py`
- [x] 生成 `data/inject/logic` 下的标准化训练/评测数据
- [x] 在 `configs/data/datasets` 注册 11 个 inject 数据集配置
- [x] 核对 WebUI 配置发现与样本量
- [x] 清理旧的错误输出路径 `data/inject/logic/test/*`
- [x] 使用 `Llama-3.2-1B-Instruct` 对 6 个逻辑数据集完成 1-step LoRA 烟雾验证

## 关键决策

- 输出结构复用现有 BREP 模式：`train/<dataset>/train.json`、`eval/<dataset>/test.json`、`manifest.json`
- 统一使用 `AlpacaDataset` / `InjectDataset` 可直接消费的字段：`instruction`、`input`、`output`、`answer`
- FOLIO 的公开可用拆分使用 `validation` 作为本地 `eval/test.json`
- RuleArena 只注册评测集，不注册训练集
- LoRA 烟雾测试统一使用真实 `Llama-3.2-1B-Instruct` 本地快照；当前 `.venv` 无法识别 CUDA，因此本轮验证在 CPU 上完成
